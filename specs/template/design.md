# 技术方案：[功能名]

## 架构决策
（为什么选这个方案而不是别的？列出备选方案和取舍理由）

## 配置格式

```yaml
# 新增/修改的配置示例
```

## 数据流

```
数据从哪里来 → 经过什么处理 → 到哪里去
```

## 接口设计

### 新增文件

| 文件路径 | 说明 |
|----------|------|
| path/to/file.py | 功能描述 |

### 函数/类签名

```python
def new_function(param: Type) -> ReturnType:
    """做什么"""
```

### 修改文件

| 文件路径 | 改动点 |
|----------|--------|
| path/to/file.py | 在XX处增加YY |

## 错误处理

| 场景 | 处理方式 |
|------|----------|
| 场景1 | 记录日志/跳过/重试 |

## 兼容性

- 是否影响现有配置？向后兼容吗？
- 默认行为是什么？
