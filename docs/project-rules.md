# 项目特定规则

> 项目初始化后必须填写本章节。模板中的内容是示例，按实际情况替换。

## Agent 角色

| 角色 | Agent | 说明 |
|------|-------|------|
| 架构 Agent | （填写） | 需求分析、方案设计、任务拆解、验收 |
| 实施 Agent | （填写） | 按 tasks.md 逐条实现 |

## 环境

- 语言/运行时：（填写，如 Python 3.11）
- 包管理器：（填写）
- 关键依赖：（填写）

## 实施调用方式

（填写实施 Agent 的调用命令，例如：）
```bash
claude --print --dangerously-skip-permissions
```

## 项目特定约束

（填写项目特有的规则，例如：）
- 所有 API 调用必须设置 proxy
- 配置文件统一用 YAML
- ...
